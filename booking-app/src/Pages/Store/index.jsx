import React from 'react';
import PropTypes from 'prop-types';

Store.propTypes = {
    
};

function Store(props) {
    return (
        <div>
            <h3>Đây là store</h3>
        </div>
    );
}

export default Store;